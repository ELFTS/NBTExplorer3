<Window x:Class="NBTExplorer.Wpf.Views.HexEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:NBTExplorer.Wpf.Controls"
        mc:Ignorable="d"
        Title="Hex Editor"
        Height="450"
        Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TabControl x:Name="viewTabs" Grid.Row="0">
            <TabItem Header="Text View">
                <TextBox x:Name="textBox" 
                         FontFamily="Courier New" 
                         FontSize="12" 
                         Margin="5" 
                         AcceptsReturn="True" 
                         TextWrapping="NoWrap" 
                         VerticalScrollBarVisibility="Auto" 
                         HorizontalScrollBarVisibility="Auto"/>
            </TabItem>
            <TabItem Header="Hex View">
                <controls:HexEditorControl x:Name="hexEditor"/>
            </TabItem>
        </TabControl>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button x:Name="importButton" Content="Import" Width="75"/>
            <Button x:Name="exportButton" Content="Export" Width="75"/>
            <Button x:Name="okButton" Content="OK" Width="75" IsDefault="True"/>
            <Button x:Name="cancelButton" Content="Cancel" Width="75" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>