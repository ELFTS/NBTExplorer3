<Window x:Class="NBTExplorer.Wpf.Views.FindReplaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NBTExplorer.Wpf.ViewModels"
        xmlns:nbt="clr-namespace:Substrate.Nbt;assembly=Substrate"
        mc:Ignorable="d"
        Title="Find and Replace"
        Height="600"
        Width="800"
        WindowStartupLocation="CenterOwner">
    <Window.DataContext>
        <vm:FindReplaceViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <ToolBar Grid.Row="0">
            <Menu>
                <MenuItem Header="Find">
                    <MenuItem Header="Add Byte Rule" Command="{Binding AddByteRuleCommand}"/>
                    <MenuItem Header="Add Short Rule" Command="{Binding AddShortRuleCommand}"/>
                    <MenuItem Header="Add Int Rule" Command="{Binding AddIntRuleCommand}"/>
                    <MenuItem Header="Add Long Rule" Command="{Binding AddLongRuleCommand}"/>
                    <MenuItem Header="Add Float Rule" Command="{Binding AddFloatRuleCommand}"/>
                    <MenuItem Header="Add Double Rule" Command="{Binding AddDoubleRuleCommand}"/>
                    <MenuItem Header="Add String Rule" Command="{Binding AddStringRuleCommand}"/>
                    <Separator/>
                    <MenuItem Header="Add Match Any Rule" Command="{Binding AddWildcardRuleCommand}"/>
                    <Separator/>
                    <MenuItem Header="Add Match All Group" Command="{Binding AddIntersectRuleCommand}"/>
                    <MenuItem Header="Add Match Any Group" Command="{Binding AddUnionRuleCommand}"/>
                    <Separator/>
                    <MenuItem Header="Edit Rule" Command="{Binding EditRuleCommand}"/>
                    <MenuItem Header="Delete Rule" Command="{Binding DeleteRuleCommand}"/>
                </MenuItem>
                <MenuItem Header="Replace">
                    <MenuItem Header="Add Byte" Command="{Binding AddByteReplaceCommand}"/>
                    <MenuItem Header="Add Short" Command="{Binding AddShortReplaceCommand}"/>
                    <MenuItem Header="Add Int" Command="{Binding AddIntReplaceCommand}"/>
                    <MenuItem Header="Add Long" Command="{Binding AddLongReplaceCommand}"/>
                    <MenuItem Header="Add Float" Command="{Binding AddFloatReplaceCommand}"/>
                    <MenuItem Header="Add Double" Command="{Binding AddDoubleReplaceCommand}"/>
                    <MenuItem Header="Add String" Command="{Binding AddStringReplaceCommand}"/>
                    <MenuItem Header="Add Byte Array" Command="{Binding AddByteArrayReplaceCommand}"/>
                    <MenuItem Header="Add Int Array" Command="{Binding AddIntArrayReplaceCommand}"/>
                    <MenuItem Header="Add Long Array" Command="{Binding AddLongArrayReplaceCommand}"/>
                    <MenuItem Header="Add List" Command="{Binding AddListReplaceCommand}"/>
                    <MenuItem Header="Add Compound" Command="{Binding AddCompoundReplaceCommand}"/>
                    <Separator/>
                    <MenuItem Header="Edit Tag" Command="{Binding EditReplaceCommand}"/>
                    <MenuItem Header="Delete Tag" Command="{Binding DeleteReplaceCommand}"/>
                </MenuItem>
            </Menu>
        </ToolBar>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Find Rules -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <DockPanel>
                    <Label DockPanel.Dock="Top" Content="Find Rules" FontWeight="Bold" Padding="5"/>
                    <TreeView ItemsSource="{Binding FindRules}" SelectedItemChanged="TreeView_SelectedItemChanged">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate DataType="{x:Type vm:RuleViewModel}" ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding Display}"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </DockPanel>
            </Border>
            
            <!-- Replace Tags -->
            <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <DockPanel>
                    <Label DockPanel.Dock="Top" Content="Replacement Tags" FontWeight="Bold" Padding="5"/>
                    <TreeView ItemsSource="{Binding ReplaceTags}" SelectedItemChanged="ReplaceTreeView_SelectedItemChanged">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate DataType="{x:Type vm:NodeViewModel}" ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding Display}"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </DockPanel>
            </Border>
        </Grid>
        
        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <CheckBox Content="Delete existing tags" IsChecked="{Binding DeleteExistingTags}"/>
            <Button Content="Find" Command="{Binding FindCommand}" Width="100"/>
            <Button Content="Replace" Command="{Binding ReplaceCommand}" Width="100"/>
            <Button Content="Replace All" Command="{Binding ReplaceAllCommand}" Width="100"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>