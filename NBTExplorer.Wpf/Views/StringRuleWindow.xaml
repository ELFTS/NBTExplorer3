<Window x:Class="NBTExplorer.Wpf.Views.StringRuleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit String Tag Rule"
        Height="300"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0" Content="Tag Name:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding TagName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
        
        <Label Grid.Row="1" Grid.Column="0" Content="Operator:"/>
        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Operators}" SelectedItem="{Binding Operator, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
        
        <Label Grid.Row="2" Grid.Column="0" Content="Value:"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TagValue, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
        
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="OK" Command="{Binding OkCommand}" Width="100"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>