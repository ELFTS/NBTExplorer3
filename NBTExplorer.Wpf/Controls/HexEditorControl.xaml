<UserControl x:Class="NBTExplorer.Wpf.Controls.HexEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                
                <ListView x:Name="AddressListView" Grid.Column="0"
                          ItemsSource="{Binding Addresses}"
                          BorderThickness="0"
                          IsHitTestVisible="False"
                          ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Courier New" FontSize="12"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                
                <ListView x:Name="HexListView" Grid.Column="1"
                          ItemsSource="{Binding HexLines}"
                          BorderThickness="0"
                          ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Courier New" FontSize="12"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                
                <ListView x:Name="AsciiListView" Grid.Column="2"
                          ItemsSource="{Binding AsciiLines}"
                          BorderThickness="0"
                          IsHitTestVisible="False"
                          ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Courier New" FontSize="12"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </ScrollViewer>
        
        <StatusBar Grid.Row="1" Height="25">
            <StatusBarItem>
                <TextBlock x:Name="PositionTextBlock" Text="Position: 0x0000"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text=" | "/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="ElementTextBlock" Text="Element: 0"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text=" | "/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="ModeTextBlock" Text="Mode: Overwrite"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>